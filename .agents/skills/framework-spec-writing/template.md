# {機能名} 仕様書

> **対象モジュール**: `src/nyxpy/framework/core/{module}/`
> **目的**: ──
> **関連ドキュメント**: ──

<!-- 既存機能の改修の場合:
> **既存ソース**: src/nyxpy/framework/core/xxx/yyy.py
> **破壊的変更**: あり / なし
-->

## 1. 概要

### 1.1 目的

<!-- 1〜3 文で機能の目的を明記。フレームワーク層の責務を明確にする。 -->

### 1.2 用語定義

| 用語 | 定義 |
|------|------|
| Command | マクロがハードウェア操作を行うための高レベル API |
| <!-- 追加 --> | <!-- 定義 --> |

### 1.3 背景・問題

<!-- 現状の課題やリファクタリング動機を簡潔に記述 -->

### 1.4 期待効果

| 指標 | 現状 | 目標 |
|------|------|------|
| <!-- 指標 --> | <!-- 値 --> | <!-- 値 --> |

### 1.5 着手条件

- <!-- 前提となる他の仕様や機能 -->
- 既存テスト (`uv run pytest tests/unit/`) がすべてパスすること

## 2. 対象ファイル

| ファイル | 変更種別 | 変更内容 |
|----------|----------|----------|
| `src/nyxpy/framework/core/xxx/yyy.py` | 新規 | ── |
| `tests/unit/test_xxx.py` | 新規 | ── |

## 3. 設計方針

### アーキテクチャ上の位置づけ

<!-- 変更対象が全体アーキテクチャのどこに位置するか -->

### 公開 API 方針

<!-- 新規 API の合理性、既存 API との整合性 -->

### 後方互換性

<!-- 破壊的変更の有無と移行計画。変更なしの場合は「破壊的変更なし」と明記 -->

### レイヤー構成

<!-- モジュール分割方針、依存方向 -->

### 性能要件

| 指標 | 目標値 |
|------|--------|
| <!-- 指標 --> | <!-- 値 --> |

### 並行性・スレッド安全性

<!-- 必要に応じてロック戦略やスレッドモデルを記述。不要なら「単一スレッドで動作」と明記 -->

## 4. 実装仕様

### 公開インターフェース

```python
class NewInterface(ABC):
    """──"""

    @abstractmethod
    def method(self) -> None:
        """──"""
        ...
```

### 設定パラメータ

| パラメータ | 型 | デフォルト | 説明 |
|------------|-----|-----------|------|
| <!-- name --> | <!-- type --> | <!-- default --> | <!-- desc --> |

### エラーハンドリング

| 例外クラス | 発生条件 |
|------------|----------|
| <!-- 例外名 --> | <!-- 条件 --> |

### シングルトン管理

<!-- singletons.py への登録が必要な場合は記述。不要なら「該当なし」 -->

## 5. テスト方針

| テスト種別 | テスト名 | 検証内容 |
|------------|----------|----------|
| ユニット | `test_xxx` | ── |
| 結合 | `test_xxx_integration` | ── |
| ハードウェア | `test_xxx_device` | `@pytest.mark.realdevice` |
| パフォーマンス | `test_xxx_perf` | ── |

## 6. 実装チェックリスト

- [ ] 公開 API のシグネチャ確定
- [ ] 内部実装
- [ ] 型ヒントの整合性チェック（ruff）
- [ ] 既存テストが破壊されないことの確認
- [ ] ユニットテスト作成・パス
- [ ] 統合テスト作成・パス
- [ ] ドキュメントコメント（公開 API のみ）
